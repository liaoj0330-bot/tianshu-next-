# TianShu P3 主动项目辅助｜验收报告

日期：2026-07-14  
范围：仅 TianShu Next 隔离实现。Teacher PPT、069、070 和业务项目未读取、未修改、未派发。

## 结论

P3 最小闭环通过工程验收，可以进入真实的非业务项目只读试点。它还不是长期运行系统，也不开放业务项目自动写入。

## 已完成

- 项目登记：项目 ID、名称、根目录、用途和默认风险等级。
- 允许路径：项目根目录内的允许路径白名单。
- 最小上下文：只读取登记的上下文文件。
- 证据追踪：每个上下文文件和整包上下文都有 SHA-256。
- SQLite 持久化：项目登记和路径白名单可写入并重新加载。
- 主动判断卡：区分结论、事实、不确定项和唯一下一步。
- 权限门：L0/L1 可在登记范围内检查；L2/L3 没有明确项目批准时拒绝。
- 安全边界：判断卡固定为 `read_only`，业务写入固定为 `closed`。

## 验收证据

`npm test`：**23/23 通过**。

覆盖内容：

- 项目注册和最小上下文哈希；
- 越界上下文拒绝；
- 越界操作拒绝；
- L2/L3 审批门；
- 主动判断卡和唯一下一步；
- SQLite 注册表持久化与重新加载；
- 原有 P1/P2 和动态状态测试无回归。

## 未宣称完成

- 不代表天枢已经自动理解所有项目；
- 不代表已经接入正式 Obsidian 自动写回；
- 不代表已经开放业务项目执行；
- 不代表长期运行、队列、Worker 和恢复已经完成；
- 不代表 Teacher PPT 069 的任何验收结论。

## P3 当前唯一下一步

用一个不属于 Teacher PPT 的真实天枢项目资料运行一次只读判断，核验项目上下文、事实来源和唯一下一步是否准确，再决定是否扩大登记范围。
