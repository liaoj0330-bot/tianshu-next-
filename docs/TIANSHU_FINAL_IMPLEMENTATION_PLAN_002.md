# TIANSHU_FINAL_IMPLEMENTATION_PLAN_002

日期：2026-07-13  
状态：分阶段实施中  
隔离实现：`D:\AI_Workspace\00_工具隔离\TianShu_Next`

## 最终目标

天枢成为 Windows 本地个人 AI 工作操作系统的唯一任务控制平面。它负责目标契约、事实装载、计划、审批、队列、隔离执行、独立验收、创造者决策、恢复和状态写回；执行器和交互适配器不得保存第二套任务真相。

## 实施原则

1. 不修改现有 `00_TianShu_Control_Center`，直到独立实现通过迁移门。
2. 不使用教师 PPT、069、070 或其他进行中项目做验证。
3. 每阶段必须有状态证据、自动测试和明确反例。
4. SQLite 是机器状态真相；Obsidian 仅是可读镜像。
5. 进程退出码、模型自报和界面显示都不能单独证明目标完成。

## P1 可信内核

范围：Goal、Plan、Approval、Task、Run、Verification、Decision、Event。

验收门：

- Goal Contract 和已提计划不可原地修改。
- 审批必须绑定规范化计划哈希。
- 非法状态迁移被拒绝。
- Executor 只能提交结果，不能完成任务。
- 只有独立验收通过且创造者接受后，目标才能完成。
- 重启能把执行中的 Run 标记为 `recovery_required`。

## P2 真实执行器

范围：Codex CLI 适配器、发行包解析、沙箱就绪检查、日志脱敏、进程身份、超时和取消。

验收门：

- 使用完整发行包调用，不能因 PATH 缺失 helper 产生假失败。
- `exit_code=0` 但真实产物不满足目标时必须失败；文本验收必须在 Plan 中显式声明字节精确或允许的换行策略。
- 只允许登记的隔离仓和精确路径。
- Codex 日志、diff、文件快照和独立验收报告全部哈希归档。

## P3 项目与上下文治理

范围：项目注册表、事实文件白名单、最小上下文包、L0-L3 权限、禁止路径。

验收门：跨项目读取和写入默认拒绝；上下文来源、版本和哈希可追溯。

## P4 长期运行治理

范围：持久 Job、Worker 租约、资源锁、恢复、有限重试、失败案例库和并发调度。

验收门：服务重启、孤儿进程、锁竞争和部分产物均能保守恢复，不猜测成功。

## P5 交互与知识镜像

范围：Obsidian 只读镜像、Hermes/AgentHub/OpenClaw/飞书适配边界和决策界面。

验收门：所有适配器只通过 Orchestrator API 改状态，断开任一适配器不丢机器真相。

## P6 迁移与真实试点

范围：选择全新非业务试点、连续运行、备份恢复、性能和使用体验验收，然后制定旧控制中心迁移方案。

验收门：创造者明确批准迁移；未批准前不切换入口、不写业务项目。

## 当前进度

- 最小验证 Lab 已证明成功、越界拒绝、排队取消和中断恢复四条闭环。
- 真实 Codex 在完整独立发行包下成功完成单文件受控修改。
- P1 正式可信内核已完成，5 项内核不变量测试通过。
- P2 真实 Codex Executor 已完成，完整测试总数 11/11。
- TianShu Next 真实 Run 已通过独立 Git/内容验收并由创造者门接受。
- 下一阶段：P3 项目注册表、最小上下文包和 L0-L3 权限门。

## 产品阶段 B 隔离实验（不改变 P3 阶段门）

- 已实现动态状态快照、更新提案、最多三个去重问题、唯一下一步和创造者接受/纠正/拒绝门。
- 完整测试 18/18 通过，三轮受控回放无重复问题并保留历史快照。
- 第 1 轮真实状态更新已由创造者纠正确认并形成新快照；真实验收为 1/3，阶段 B 尚未通过。
- 该实验不写正式 Vault、不接业务项目，也不证明 P3 完成或产品阶段 B 通过。
- 实施报告：`docs\TIANSHU_DYNAMIC_STATE_MVP_REPORT_001.md`。
